#!/usr/bin/node
function biggest () {
  let arBig = [];
  process.argv.forEach ((val, index) {
    arBig
  });
}
if (process.argv.length === 2 || (process.argv[2] && !process.argv[3])) {
  console.log(0);
} else {
  biggest();
}
